apiVersion: v1
kind: Secret
metadata:
  name: credentials
type: Opaque
# Note: In a real deployment, these values would be generated and managed 
# securely, not stored in version control. This is just a template.
data:
  # The following values are placeholders and should be replaced
  # with actual base64-encoded secrets during deployment.
  # Example: echo -n "your-value" | base64
  database-password: UExBQ0VIT0xERVJfUEFTU1dPUkQ=  # PLACEHOLDER_PASSWORD
  redis-password: UExBQ0VIT0xERVJfUEFTU1dPUkQ=     # PLACEHOLDER_PASSWORD
  encryption-key: UExBQ0VIT0xERVJfRU5DUllQVElPTl9LRVk=  # PLACEHOLDER_ENCRYPTION_KEY
