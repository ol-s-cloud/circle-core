apiVersion: v1
kind: Secret
metadata:
  name: circle-core-secrets
  labels:
    app: circle-core
type: Opaque
# These are example placeholders only - NEVER store actual secrets in version control!
# In a real deployment, use Kubernetes Secrets management or external secret stores
stringData:
  # Database credentials
  DB_PASSWORD: "PLACEHOLDER_CHANGE_ME"
  
  # Redis credentials
  REDIS_PASSWORD: "PLACEHOLDER_CHANGE_ME"
  
  # Encryption keys (use proper key management in production)
  CIRCLE_ENCRYPTION_KEY: "PLACEHOLDER_CHANGE_ME"
  
  # API keys and authentication secrets
  API_KEY: "PLACEHOLDER_CHANGE_ME"
  JWT_SECRET: "PLACEHOLDER_CHANGE_ME"
  MFA_SECRET: "PLACEHOLDER_CHANGE_ME"
